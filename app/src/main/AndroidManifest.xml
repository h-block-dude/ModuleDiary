<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hamzah.modulediary">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ModuleDiary"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".RegisterActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SetUp.UniversityPickerActivity"
            android:label="@string/title_activity_university_picker"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SetUp.CoursePickerActivity"
            android:label="@string/title_activity_course_picker"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SetUp.YearPickerActivity"
            android:label="@string/title_activity_year_picker"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SetUp.ModulePicker.ModulePickerActivity"
            android:label="@string/title_activity_module_picker"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".AddMoodActivity"
            android:label="@string/title_activity_add_mood"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Deadlines.AddDeadlineActivity"></activity>
    </application>

</manifest>